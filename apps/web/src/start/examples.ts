// The Start board's own five (L1). They are examples of how to phrase a thing, not a menu of
// content: against the fixtures one of them finds nothing, which is how the page shows that state.
export const EXAMPLES = [
  "Salmon",
  "Why my reads don't map",
  "Differential expression",
  "de Bruijn graphs",
  "Call variants",
] as const;
