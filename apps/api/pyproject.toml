[project]
name = "code-api"
version = "0.0.0"
description = "Comeni Code's Django project: accounts, content, studio, learn, requests and AI, behind one JSON API."
requires-python = ">=3.14"
license = "Apache-2.0"
dependencies = [
    "django>=6.1,<6.2",
    "psycopg[binary]>=3.3,<3.4",
    "pydantic-settings>=2.15,<3",
    "django-ninja>=1.7,<1.8",
    "whitenoise>=6.12,<7",
    "celery[redis]>=5.6,<5.7",
    "gunicorn>=26.2,<27",
    "code-schema",
    "code-weaver",
]

# The node format and the weaver, from this workspace: the index reads content as CI does, and
# /api/routes weaves from the index. Both must be installed in the image, not only in the dev sync.
[tool.uv.sources]
code-schema = { workspace = true }
code-weaver = { workspace = true }

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"
